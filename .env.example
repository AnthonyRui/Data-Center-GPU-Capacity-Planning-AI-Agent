# Local configuration only / 仅本地配置
LLM_PROVIDER=openrouter
LLM_API_KEY=YOUR_API_KEY
LLM_MODEL=openrouter/free
# Block paid OpenRouter models by default / 默认禁止 OpenRouter 付费模型
LLM_ALLOW_PAID_MODELS=false
# Optional settings / 可选配置
LLM_TIMEOUT_SECONDS=
LLM_MAX_TOOL_ROUNDS=
